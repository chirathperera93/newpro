import java.awt.*;


public class ADI_Config {

    // These all variables should be private and final because this variables is used only within this class and never be changed their values
    static int panelWidth = 504;
    static int panelHeight = 432;
    static Dimension panelSize = new Dimension(panelWidth, panelHeight);


    static int defaultLength = 5;
    static int snakeX = 240;
    static int snakeY = 240;

}